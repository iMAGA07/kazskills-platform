
  # STK

  This is a code bundle for STK. The original project is available at https://www.figma.com/design/b1iK3CmpMijCKtxNZCii6U/STK.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  